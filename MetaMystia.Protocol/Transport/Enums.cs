namespace MetaMystia.Protocol.Transport;

public enum TransportKind { None, DirectHost, DirectClient, RelayClient }
public enum SyncScope { None, Public, Room }
public enum RoomRole { None, Host, Client }
